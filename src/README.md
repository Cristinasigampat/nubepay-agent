# src/

Acá va a vivir el código del agente:

- Loaders/parsers por formato de documento (PDF, Word, Excel, PowerPoint, Markdown, CSV, JSON, HTML)
- Pipeline de chunking y generación de embeddings
- Conexión a la base vectorial
- Orquestación RAG y API de chat

Todavía no implementado — próxima etapa del proyecto.
